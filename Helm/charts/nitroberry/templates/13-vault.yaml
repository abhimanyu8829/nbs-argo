apiVersion: apps/v1
kind: Deployment
metadata:
  name: vault-api
  namespace: vault-namespace
  labels:
    app: vault-api
    managed-by: nitroberry
spec:
  strategy:
    type: {{ .Values.services.vault.strategyType | default "RollingUpdate" }}
  replicas: {{ .Values.services.vault.replicas }}
  selector:
    matchLabels:
      app: vault-api
  template:
    metadata:
      labels:
        app: vault-api
        managed-by: nitroberry
    spec:
      imagePullSecrets:
        - name: ecr-registry-secret
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchLabels:
                  app: vault-api
              topologyKey: kubernetes.io/hostname
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app: vault-api
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: api
          image: "{{ .Values.services.vault.api.repository }}:{{ .Values.services.vault.api.tag }}"
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: vault-api-config
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: vault-api-secrets
                  key: database-url
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: {{ .Values.probes.readinessInitialDelaySeconds }}
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: {{ .Values.probes.livenessInitialDelaySeconds }}
            periodSeconds: 20
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: pm2-runtime
              mountPath: /home/node/.pm2
            - name: app-logs
              mountPath: /app/logs
        - name: worker
          image: "{{ .Values.services.vault.worker.repository }}:{{ .Values.services.vault.worker.tag }}"
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: vault-api-config
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: vault-api-secrets
                  key: database-url
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: pm2-runtime
              mountPath: /home/node/.pm2
            - name: app-logs
              mountPath: /app/logs
      volumes:
        - name: pm2-runtime
          emptyDir: {}
        - name: app-logs
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: vault-api-service
  namespace: vault-namespace
spec:
  selector:
    app: vault-api
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: vault-api-hpa
  namespace: vault-namespace
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: vault-api
  minReplicas: {{ .Values.services.vault.hpa.minReplicas }}
  maxReplicas: {{ .Values.services.vault.hpa.maxReplicas }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: vault-api-websecure
  namespace: vault-namespace
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`vault.nitroberry.com`) && (PathPrefix(`/health`) || PathPrefix(`/public`))
      kind: Rule
      services:
        - name: vault-api-service
          port: 8080
      middlewares:
        - name: rate-limit
          namespace: traefik-ingress
        - name: security-headers
          namespace: traefik-ingress
    - match: Host(`vault.nitroberry.com`)
      kind: Rule
      services:
        - name: vault-api-service
          port: 8080
      middlewares:
        - name: jwt-auth
          namespace: traefik-ingress
        - name: rate-limit
          namespace: traefik-ingress
        - name: security-headers
          namespace: traefik-ingress
  tls:
    certResolver: myresolver
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: vault-api-policy
  namespace: vault-namespace
spec:
  podSelector:
    matchLabels:
      app: vault-api
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: traefik-ingress
    ports:
    - protocol: TCP
      port: 8080
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: database-namespace
      podSelector:
        matchLabels:
          app: pgbouncer
    ports:
    - protocol: TCP
      port: 6432
