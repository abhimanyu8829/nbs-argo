apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-api
  namespace: auth-namespace
  labels:
    app: auth-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: auth-api
  template:
    metadata:
      labels:
        app: auth-api
    spec:
      containers:
      - name: api
        image: 798701233691.dkr.ecr.ap-south-1.amazonaws.com/nitroberry/auth-api:latest
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: auth-api-config
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: auth-api-secrets
              key: database-url
      - name: worker
        image: 798701233691.dkr.ecr.ap-south-1.amazonaws.com/nitroberry/auth-worker:latest
        envFrom:
        - configMapRef:
            name: auth-api-config
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: auth-api-secrets
              key: database-url
---
apiVersion: v1
kind: Service
metadata:
  name: auth-api-service
  namespace: auth-namespace
spec:
  selector:
    app: auth-api
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: auth-api-hpa
  namespace: auth-namespace
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: auth-api
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: auth-api-websecure
  namespace: auth-namespace
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`auth.nitroberry.com`)
      kind: Rule
      services:
        - name: auth-api-service
          port: 8080
      middlewares:
        - name: rate-limit
          namespace: traefik-ingress
        - name: security-headers
          namespace: traefik-ingress
  tls:
    certResolver: myresolver
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: auth-api-policy
  namespace: auth-namespace
spec:
  podSelector:
    matchLabels:
      app: auth-api
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: traefik-ingress
    ports:
    - protocol: TCP
      port: 8080
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: database-namespace
    ports:
    - protocol: TCP
      port: 5432
