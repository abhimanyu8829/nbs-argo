apiVersion: apps/v1
kind: Deployment
metadata:
  name: social-api
  namespace: social-namespace
  labels:
    app: social-api
    managed-by: nitroberry
spec:
  strategy:
    type: {{ .Values.services.social.strategyType | default "RollingUpdate" }}
  replicas: {{ .Values.services.social.replicas }}
  selector:
    matchLabels:
      app: social-api
  template:
    metadata:
      labels:
        app: social-api
        managed-by: nitroberry
    spec:
      imagePullSecrets:
        - name: ecr-registry-secret
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchLabels:
                  app: social-api
              topologyKey: kubernetes.io/hostname
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app: social-api
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: api
          image: "{{ .Values.services.social.api.repository }}:{{ .Values.services.social.api.tag }}"
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: social-api-config
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: social-api-secrets
                  key: database-url
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: pm2-runtime
              mountPath: /home/node/.pm2
        - name: worker
          image: "{{ .Values.services.social.worker.repository }}:{{ .Values.services.social.worker.tag }}"
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: social-api-config
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: social-api-secrets
                  key: database-url
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: pm2-runtime
              mountPath: /home/node/.pm2
      volumes:
        - name: pm2-runtime
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: social-api-service
  namespace: social-namespace
spec:
  selector:
    app: social-api
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: social-api-hpa
  namespace: social-namespace
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: social-api
  minReplicas: {{ .Values.services.social.hpa.minReplicas }}
  maxReplicas: {{ .Values.services.social.hpa.maxReplicas }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: social-api-websecure
  namespace: social-namespace
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`social.nitroberry.com`)
      kind: Rule
      services:
        - name: social-api-service
          port: 8080
      middlewares:
        - name: rate-limit
          namespace: traefik-ingress
        - name: security-headers
          namespace: traefik-ingress
  tls:
    certResolver: myresolver
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: social-api-policy
  namespace: social-namespace
spec:
  podSelector:
    matchLabels:
      app: social-api
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: traefik-ingress
    ports:
    - protocol: TCP
      port: 8080
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: database-namespace
      podSelector:
        matchLabels:
          app: pgbouncer
    ports:
    - protocol: TCP
      port: 6432
