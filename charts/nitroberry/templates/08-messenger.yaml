apiVersion: apps/v1
kind: Deployment
metadata:
  name: messenger-api
  namespace: messenger-namespace
  labels:
    app: messenger-api
    managed-by: nitroberry
spec:
  strategy:
    type: {{ .Values.services.messenger.strategyType | default "RollingUpdate" }}
  replicas: {{ .Values.services.messenger.replicas }}
  selector:
    matchLabels:
      app: messenger-api
  template:
    metadata:
      labels:
        app: messenger-api
        managed-by: nitroberry
    spec:
      imagePullSecrets:
        - name: ecr-registry-secret
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchLabels:
                  app: messenger-api
              topologyKey: kubernetes.io/hostname
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app: messenger-api
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: api
          image: "{{ .Values.services.messenger.api.repository }}:{{ .Values.services.messenger.api.tag }}"
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: messenger-api-config
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: messenger-api-secrets
                  key: database-url
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: {{ .Values.probes.readinessInitialDelaySeconds }}
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: {{ .Values.probes.livenessInitialDelaySeconds }}
            periodSeconds: 20
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: pm2-runtime
              mountPath: /home/node/.pm2
            - name: app-logs
              mountPath: /app/logs
      volumes:
        - name: pm2-runtime
          emptyDir: {}
        - name: app-logs
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: messenger-api-service
  namespace: messenger-namespace
spec:
  selector:
    app: messenger-api
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: messenger-api-hpa
  namespace: messenger-namespace
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: messenger-api
  minReplicas: {{ .Values.services.messenger.hpa.minReplicas }}
  maxReplicas: {{ .Values.services.messenger.hpa.maxReplicas }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: messenger-api-websecure
  namespace: messenger-namespace
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`messenger.nitroberry.com`)
      kind: Rule
      services:
        - name: messenger-api-service
          port: 8080
      middlewares:
        - name: rate-limit
          namespace: traefik-ingress
        - name: security-headers
          namespace: traefik-ingress
  tls:
    certResolver: myresolver
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: messenger-api-policy
  namespace: messenger-namespace
spec:
  podSelector:
    matchLabels:
      app: messenger-api
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: traefik-ingress
    ports:
    - protocol: TCP
      port: 8080
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: database-namespace
      podSelector:
        matchLabels:
          app: pgbouncer
    ports:
    - protocol: TCP
      port: 6432
