apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-api
  namespace: users-namespace
spec:
  replicas: 2
  selector:
    matchLabels:
      app: users-api
  template:
    metadata:
      labels:
        app: users-api
    spec:
      containers:
      - name: api
        image: nitroberry/users-api:latest
        ports:
        - containerPort: 8080
        env:
        - name: DATABASE_URL
          value: "postgres://postgres:nitroberry-secret-password@postgres-service.database-namespace.svc.cluster.local:5432/postgres?options=-csearch_path%3Dusers"
---
apiVersion: v1
kind: Service
metadata:
  name: users-api-service
  namespace: users-namespace
spec:
  selector:
    app: users-api
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: users-api-hpa
  namespace: users-namespace
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: users-api
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: users-api-websecure
  namespace: users-namespace
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`users.nitroberry.com`) && (PathPrefix(`/health`) || PathPrefix(`/public`))
      kind: Rule
      services:
        - name: users-api-service
          port: 8080
      middlewares:
        - name: rate-limit
          namespace: traefik-ingress
        - name: security-headers
          namespace: traefik-ingress
    - match: Host(`users.nitroberry.com`)
      kind: Rule
      services:
        - name: users-api-service
          port: 8080
      middlewares:
        - name: jwt-auth
          namespace: traefik-ingress
        - name: rate-limit
          namespace: traefik-ingress
        - name: security-headers
          namespace: traefik-ingress
  tls:
    certResolver: myresolver
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: users-api-policy
  namespace: users-namespace
spec:
  podSelector:
    matchLabels:
      app: users-api
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: traefik-ingress
    ports:
    - protocol: TCP
      port: 8080
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: database-namespace
    ports:
    - protocol: TCP
      port: 5432
