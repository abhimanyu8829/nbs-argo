apiVersion: apps/v1
kind: Deployment
metadata:
  name: messenger-api
  namespace: messenger-namespace
  labels:
    app: messenger-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: messenger-api
  template:
    metadata:
      labels:
        app: messenger-api
    spec:
      containers:
      - name: api
        image: 798701233691.dkr.ecr.ap-south-1.amazonaws.com/nitroberry/messenger-api:latest
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: messenger-api-config
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: messenger-api-secrets
              key: database-url
---
apiVersion: v1
kind: Service
metadata:
  name: messenger-api-service
  namespace: messenger-namespace
spec:
  selector:
    app: messenger-api
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: messenger-api-hpa
  namespace: messenger-namespace
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: messenger-api
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: messenger-api-websecure
  namespace: messenger-namespace
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`messenger.nitroberry.com`)
      kind: Rule
      services:
        - name: messenger-api-service
          port: 8080
      middlewares:
        - name: rate-limit
          namespace: traefik-ingress
        - name: security-headers
          namespace: traefik-ingress
  tls:
    certResolver: myresolver
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: messenger-api-policy
  namespace: messenger-namespace
spec:
  podSelector:
    matchLabels:
      app: messenger-api
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: traefik-ingress
    ports:
    - protocol: TCP
      port: 8080
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: database-namespace
    ports:
    - protocol: TCP
      port: 5432