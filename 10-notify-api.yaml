apiVersion: apps/v1
kind: Deployment
metadata:
  name: notify-api
  namespace: notify-namespace
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notify-api
  template:
    metadata:
      labels:
        app: notify-api
    spec:
      containers:
      - name: api
        image: nitroberry/notify-api:latest
        ports:
        - containerPort: 8080
        env:
        - name: DATABASE_URL
          value: "postgres://postgres:nitroberry-secret-password@postgres-service.database-namespace.svc.cluster.local:5432/postgres?options=-csearch_path%3Dnotify"
---
apiVersion: v1
kind: Service
metadata:
  name: notify-api-service
  namespace: notify-namespace
spec:
  selector:
    app: notify-api
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: notify-api-hpa
  namespace: notify-namespace
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: notify-api
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: notify-api-websecure
  namespace: notify-namespace
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`notify.nitroberry.com`) && (PathPrefix(`/health`) || PathPrefix(`/public`))
      kind: Rule
      services:
        - name: notify-api-service
          port: 8080
      middlewares:
        - name: rate-limit
          namespace: traefik-ingress
        - name: security-headers
          namespace: traefik-ingress
    - match: Host(`notify.nitroberry.com`)
      kind: Rule
      services:
        - name: notify-api-service
          port: 8080
      middlewares:
        - name: jwt-auth
          namespace: traefik-ingress
        - name: rate-limit
          namespace: traefik-ingress
        - name: security-headers
          namespace: traefik-ingress
  tls:
    certResolver: myresolver
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: notify-api-policy
  namespace: notify-namespace
spec:
  podSelector:
    matchLabels:
      app: notify-api
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: traefik-ingress
    ports:
    - protocol: TCP
      port: 8080
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: database-namespace
    ports:
    - protocol: TCP
      port: 5432
